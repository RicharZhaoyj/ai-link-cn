# Vercel Python 环境依赖
# 注意：Vercel 对包大小有限制，这里只包含核心依赖

# 核心数据分析（必须）
pandas==2.0.3
numpy==1.24.3

# 轻量级 HTTP 服务
bottle==0.12.25

# 如果需要 API 功能，可以添加以下依赖
# yfinance==0.2.28  # 注意：Vercel 可能不支持 yfinance
# ccxt==4.0.0       # 注意：Vercel 可能不支持 ccxt

# 开发依赖（可选）
python-dateutil==2.8.2
pytz==2023.3